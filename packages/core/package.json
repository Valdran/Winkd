{
  "name": "@winkd/core",
  "version": "0.0.1",
  "private": true,
  "description": "Winkd shared business logic — encryption, protocol, contact management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@winkd/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
